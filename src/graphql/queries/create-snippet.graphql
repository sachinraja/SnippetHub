mutation CreateSnippet(
  $packId: Int!
  $snippet: SnippetInput!
) {
  createSnippet(
    packId: $packId
    snippet: $snippet
  ) {
    id
    name
    code
    language
    packId
    createdAt
    updatedAt
  }
}
