query GetPackByName($name: String!, $skip: Int, $take: Int) {
  getPackByName(name: $name, skip: $skip, take: $take) {
    author {
      username
      image
    }
    id
    name
    shortDescription
    language
    upvotes
  }
}
