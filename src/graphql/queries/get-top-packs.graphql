query GetTopPacks($skip: Int, $take: Int) {
  getTopPacks(skip: $skip, take: $take) {
    author {
      username
      image
    }
    id
    name
    shortDescription
    language
    upvotes
  }
}
