mutation UpdatePackShortDescription(
  $packId: Int!
  $packShortDescription: String!
) {
  updatePackShortDescription(
    id: $packId
    shortDescription: $packShortDescription
  ) {
    name
  }
}
